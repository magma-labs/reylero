#!/bin/sh

set -e

if [ "$HUBOT_ENV" = "production" ]; then
  npm install
fi

export PATH="node_modules/.bin:node_modules/hubot/node_modules/.bin:$PATH"

exec node_modules/.bin/hubot --name "reylero" "$@"
